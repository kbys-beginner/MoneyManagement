package com.example.moneymanagement

import android.app.AlertDialog
import android.content.Context
import android.view.LayoutInflater
import com.bumptech.glide.Glide
import com.example.moneymanagement.databinding.LayoutLoadingBinding

object Load {
//    private lateinit var loadDialog: AlertDialog
//    fun start(context: Context) {
//        loadDialog = AlertDialog.Builder(context).apply {
//            setCancelable(false)
//            setView(LayoutLoadingBinding.inflate(LayoutInflater.from(context)).apply {
//                Glide.with(context).load(R.raw.cat).into(imageView)
//            }.root)
//        }.create()
//        loadDialog.show()
//    }
//    fun stop(){
//        loadDialog.dismiss()
//    }
}